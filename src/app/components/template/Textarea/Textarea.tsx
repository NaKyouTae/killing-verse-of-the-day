
// @ts-ignore
function Textarea() {
    const change = (e: any) => {
    };

    const summit = () => {

    }

    return (
        <div className={"textarea-container"}>
            <textarea />
            <button className={"primary-button"}>응원</button>
        </div>
    );
};

export default Textarea;
